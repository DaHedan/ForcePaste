# ForcePaste 软件需求文档

## 1. 概述

### 1.1 软件定位

轻量级桌面工具，一键将剪贴板文本以模拟键入方式输入目标程序，绕过粘贴限制。

### 1.2 核心价值

- 绕过粘贴限制，恢复用户对文本输入的掌控
- 不修改目标程序，无需管理员权限
- 零侵入：仅模拟键盘事件，不注入、不 Hook 目标进程

---

## 2. 功能需求

### 2.1 核心流程

```
读取剪贴板文本 → 用户确认 → 模拟键盘逐字符输入
```

### 2.2 触发方式

| 方式 | 说明 |
|------|------|
| 全局热键 | 默认 `Ctrl+Shift+V`，支持自定义 |
| 托盘菜单 | 右键托盘图标 → 「立即输入」 |

### 2.3 输入控制

- **输入速度**：字符间延迟可调，默认 30ms，范围 5–500ms
- **中文输入**：逐段复制到剪贴板 + 模拟 `Ctrl+V`，或通过 `SendInput` 发送 Unicode 事件
- **特殊字符**：支持符号、制表符（Tab）、换行（Enter）等
- **换行处理**：文本中的 `\n` 映射为 `Enter` 键

### 2.4 剪贴板预览

- 触发后弹出预览，显示剪贴板前 100 字
- 用户确认后立即开始模拟键入，取消则终止

### 2.5 输入状态

- 输入进行中显示半透明悬浮进度窗（当前进度/总字符数）
- 按 `ESC` 可随时中止输入
- 输入完成后悬浮窗自动消失

### 2.6 历史记录（可选）

- 记录最近使用的文本，最多 20 条
- 可从托盘菜单或设置面板选择历史条目重新输入
- 不记录剪贴板中的图片等非文本内容

---

## 3. 非功能需求

| 指标 | 要求 |
|------|------|
| 分发形式 | 单文件绿色软件，无需安装 |
| 体积 | < 5MB |
| 启动速度 | < 2s |
| 运行形态 | 启动后最小化到系统托盘，不占任务栏 |
| 内存占用 | < 30MB |
| 操作系统 | Windows 10 / 11（64-bit） |
| 界面语言 | 中文 / 英文可切换 |
| 开源协议 | GPL-3.0 |

---

## 4. 技术方案建议

### 4.1 技术选型

- **语言**：C# (.NET 8)
- **UI 框架**：WPF
- **发布方式**：单文件发布（`PublishSingleFile`），无需安装

> WPF 提供原生 Win32 互操作能力，UI 表现力强，适合设置面板与悬浮窗开发。

### 4.2 关键模块

| 模块 | 实现方式 |
|------|----------|
| 模拟键入 | Win32 `SendInput` API，发送 `KEYEVENTF_UNICODE` 事件 |
| 中文输入 | `SendInput` 发送 Unicode 字符，无需依赖输入法 |
| 全局热键 | Win32 `RegisterHotKey` / `UnregisterHotKey` |
| 系统托盘 | Win32 `Shell_NotifyIcon` |
| 剪贴板读取 | Win32 `OpenClipboard` / `GetClipboardData` |
| 悬浮窗 | WPF 顶层无边框窗口，`WS_EX_TOPMOST` + `WS_EX_NOACTIVATE` |

---

## 5. 界面设计

### 5.1 主窗口（设置面板）

简洁单页设置，包含：

- 热键设置（热键录制输入框）
- 输入速度滑块（5–500ms）
- 语言切换（中文/English）
- 历史记录列表（可选）
- 开机自启动开关

### 5.2 系统托盘

- 默认图标：凿子样式
- 左键双击：打开设置面板
- 右键菜单：

```
├─ 立即输入
├─ 设置...
├─ ─────────
├─ 关于
└─ 退出
```

### 5.3 输入提示悬浮窗

- 半透明置顶窗口，不抢占焦点（`WS_EX_NOACTIVATE`）
- 显示：当前字符 / 总字符数 + 进度条
- 输入完成后 1.5s 自动淡出

---

## 6. 使用流程

```
1. 复制要输入的文本 → 剪贴板
2. 切换到目标窗口，将光标放入输入框
3. 按下全局热键（如 Ctrl+Shift+V）
4. 预览剪贴板内容（前100字），确认
5. 开始模拟键入 → ESC 可中止
```

---

## 7. 附录

### 7.1 术语表

| 术语 | 含义 |
|------|------|
| 模拟键入 | 通过操作系统 API 发送键盘事件，与物理按键等效 |
| 全局热键 | 系统级快捷键，无论当前焦点在哪个窗口均可响应 |
| 绿色软件 | 无需安装，单文件即可运行，不写注册表 |

### 7.2 风险与约束

- 部分高安全级别程序可能拦截模拟按键事件（如银行控件），此为系统层面限制，非软件 Bug
- 以管理员权限运行目标程序时，ForcePaste也需以管理员权限运行，否则模拟按键无法送达
- 长文本输入期间请勿操作键盘鼠标，否则可能干扰模拟输入
